name: Model
type: library
description: "TypeScript type definitions for all domain types — graph, context, validation, drift"
aspects: []

relations: []

mapping:
  paths:
    - source/cli/src/model/
