name: NodeSelector
type: library
description: "Task-based node selection using keyword matching against graph artifacts"
aspects:
  - aspect: deterministic

relations:
  - target: cli/model
    type: uses
    consumes: [Graph, GraphNode, FlowDef]
  - target: cli/utils
    type: uses
    consumes: [tokenize]

mapping:
  paths:
    - source/cli/src/core/node-selector.ts
