name: DependencyResolver
type: library
description: "Topological sort, change detection, and dependency tree formatting for graph nodes"
aspects:
  - aspect: deterministic

relations:
  - target: cli/model
    type: uses
    consumes: [Graph, Stage]

mapping:
  paths:
    - source/cli/src/core/dependency-resolver.ts
