name: Validator
type: library
tags: [deterministic]

relations:
  - target: cli/core/context
    type: uses
    consumes: [buildContext]
  - target: cli/model
    type: uses
    consumes: [Graph, ValidationResult, ValidationIssue, ArtifactConfig]
  - target: cli/utils
    type: uses
    consumes: [normalizeMappingPaths, getLastCommitTimestamp]

knowledge:
  - invariants/001-context-package-sufficient
  - invariants/002-graph-intended-truth
  - decisions/001-cli-tools-read-only

mapping:
  paths:
    - source/cli/src/core/validator.ts
