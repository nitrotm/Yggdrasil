name: ContextBuilder
type: library
tags: [deterministic]

relations:
  - target: cli/model
    type: uses
    consumes: [Graph, GraphNode, ContextPackage, ContextLayer, ContextSection, YggConfig, AspectDef, KnowledgeItem, FlowDef, Relation]
  - target: cli/utils
    type: uses
    consumes: [normalizeMappingPaths, estimateTokens]

knowledge:
  - decisions/002-structural-context
  - invariants/001-context-package-sufficient

mapping:
  paths:
    - source/cli/src/core/context-builder.ts
