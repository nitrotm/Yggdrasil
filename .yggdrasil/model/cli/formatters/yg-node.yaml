name: Formatters
type: library
aspects:
  - aspect: deterministic

relations:
  - target: cli/model
    type: uses
    consumes: [ContextPackage, ContextMapOutput]

mapping:
  paths:
    - source/cli/src/formatters/
