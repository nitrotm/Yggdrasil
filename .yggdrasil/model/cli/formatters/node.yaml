name: Formatters
type: library
tags: [deterministic]

relations:
  - target: cli/model
    type: uses
    consumes: [ContextPackage]

knowledge: []

mapping:
  paths:
    - source/cli/src/formatters/
