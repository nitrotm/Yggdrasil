name: DriftCommands
type: library
aspects: [deterministic, cli-command-contract]

relations:
  - target: cli/core/loader
    type: uses
    consumes: [loadGraph]
  - target: cli/core/drift-detector
    type: uses
    consumes: [detectDrift, syncDriftState]

mapping:
  paths:
    - source/cli/src/cli/drift.ts
    - source/cli/src/cli/drift-sync.ts
