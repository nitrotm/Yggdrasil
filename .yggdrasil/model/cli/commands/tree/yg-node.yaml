name: TreeCommand
type: library
aspects:
  - aspect: deterministic
  - aspect: cli-command-contract

relations:
  - target: cli/core/loader
    type: uses
    consumes: [loadGraph]
  - target: cli/model
    type: uses
    consumes: [GraphNode]

mapping:
  paths:
    - source/cli/src/cli/tree.ts
