name: InitCommand
type: library
tags: [deterministic]

relations:
  - target: cli/templates
    type: uses
    consumes: [DEFAULT_CONFIG, installRulesForPlatform, PLATFORMS, Platform]

knowledge:
  - patterns/command-error-handling

mapping:
  paths:
    - source/cli/src/cli/init.ts
