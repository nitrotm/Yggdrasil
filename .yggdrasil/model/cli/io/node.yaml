name: IO
type: library
aspects: [deterministic, posix-paths, yaml-parser-contract, silent-missing-files]

relations:
  - target: cli/model
    type: uses
    consumes:
      - YggConfig
      - ArtifactConfig
      - QualityConfig
      - NodeMeta
      - NodeMapping
      - Relation
      - RelationType
      - Artifact
      - FlowDef
      - AspectDef
      - SchemaDef
      - DriftState
      - DriftNodeState
      - JournalEntry

mapping:
  paths:
    - source/cli/src/io/
