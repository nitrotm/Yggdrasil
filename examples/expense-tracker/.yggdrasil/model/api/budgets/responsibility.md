# Budgets — Responsibility

CRUD for monthly budgets per category. List returns current spend vs limit. Upsert: one budget per (user, category, month).

Not responsible for: expense creation, budget exceeded alerts (expenses service).
