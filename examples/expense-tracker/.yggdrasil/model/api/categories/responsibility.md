# Categories — Responsibility

CRUD for categories. List: predefined + user-owned. Create: limit 5 custom on Free plan. Edit and delete: only user-owned (user_id = userId).

Not responsible for: predefined categories (seed in db).
