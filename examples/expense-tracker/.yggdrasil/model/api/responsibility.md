# API — Responsibility

Backend module for Expense Tracker. Provides REST API for auth, users, subscriptions, expenses, categories, budgets, and reports. Uses Fastify, SQLite, JWT for auth.

Not responsible for: frontend rendering, business logic outside API boundaries.
