# Expenses — Responsibility

List, add, edit expenses. Filters by month. Handles budget_exceeded toast. Handles 403 (limit) with upgrade prompt.

Not responsible for: validation (API), limit enforcement (API).
